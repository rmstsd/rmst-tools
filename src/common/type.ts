export type SettingData = {
  vscodePath: string
  cmdPath: string
  projectPaths: string[]
  notes: string[]
}

export type AppBaseInfo = {
  appPath: string
  appVersion: string
  appName: string
  node: string
  chrome: string
  electron: string
}
