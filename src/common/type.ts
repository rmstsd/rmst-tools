export type SettingData = {
  editorPaths: string[]
  cmdPath: string
  projectPaths: string[]
  notes: string[]
  historyOpenedUrls: string[]
}

export type AppBaseInfo = {
  appPath: string
  appVersion: string
  appName: string
  node: string
  chrome: string
  electron: string
}
